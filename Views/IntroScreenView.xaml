<UserControl x:Class="Mythic.Views.IntroScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920"
             Focusable="True"
             IsHitTestVisible="True"
             Background="Black">
    
    <UserControl.Resources>
        <Storyboard x:Key="FadeOutAnimation">
            <DoubleAnimation Storyboard.TargetName="IntroContainer"
                           Storyboard.TargetProperty="Opacity"
                           To="0"
                           Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <Grid x:Name="IntroContainer">
        <!-- Video Player -->
        <MediaElement x:Name="IntroVideo"
                     LoadedBehavior="Manual"
                     UnloadedBehavior="Stop"
                     Stretch="Uniform"
                     MediaEnded="IntroVideo_MediaEnded"
                     MediaFailed="IntroVideo_MediaFailed"
                     Volume="1.0" />
        
        <!-- Skip Hint Text with Background -->
        <Border HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="20"
                Background="#40000000"
                Padding="10,5"
                CornerRadius="4">
            <TextBlock x:Name="SkipHint"
                      Text="Press any button to skip"
                      Foreground="White"
                      FontSize="14"
                      Opacity="0.9" />
        </Border>
    </Grid>
</UserControl>
